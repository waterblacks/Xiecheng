# ==================== 通用 Node.js 忽略项（前后端共用） ====================
# 依赖包（体积大，可通过 npm install 重建，无需提交）
node_modules/
# npm 日志文件
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# pnpm 存储目录
.pnpm-store/
# 包管理锁文件（可选：如果团队统一锁版本，可取消注释；否则忽略）
# package-lock.json
# yarn.lock
# pnpm-lock.yaml
# Node.js 进程文件
*.pid
*.seed
*.pid.lock

# ==================== React 前端专属忽略项 ====================
# React 构建产物（npm run build 生成的静态文件，无需提交）
build/
# create-react-app 生成的开发缓存
/.env.local
/.env.development.local
/.env.test.local
/.env.production.local
# 开发环境缓存
/.cache/
# ES lint 缓存
.eslintcache
# 编辑器临时文件（比如 VS Code）
.vscode/
# macOS 系统文件
.DS_Store
# Windows 快捷方式/临时文件
Thumbs.db
*.tmp
*.bak

# ==================== Node.js 后端专属忽略项 ====================
# 后端环境变量文件（包含数据库密码、密钥等敏感信息，必须忽略）
.env
# 后端日志目录
logs/
# 后端上传文件目录
uploads/
# 后端编译产物（比如 TypeScript 编译后的 dist）
dist/
# PM2 进程管理配置（可选，若用 PM2 部署）
ecosystem.config.js
.pm2/
.idea/